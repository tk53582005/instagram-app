// コメントリストの先頭に新しいコメントを追加
document.getElementById('comments-list').insertAdjacentHTML('afterbegin', '<%= j render @comment %>');

// フォームをクリア
document.getElementById('comment-form-<%= @post.id %>').reset();

// コメント数を更新
const commentCount = document.querySelector('#like-button-<%= @post.id %> span:nth-child(2)');
if (commentCount) {
  commentCount.innerHTML = '💬 <%= @post.comments.count %>';
}